<h2><%= @application.name %> Metrics</h2>

<div class='row'>
  <div class='col-md-12'>
    
      <h3><%= @application.name %></h3>
      <h3><%= @application.url %></h3>
      
      <%= link_to "Remove Monitoring for this Site", [@application], data: {confirm: "This action cannot be undone. Are you sure?" }, method: :delete, class: 'btn btn-danger' %>
    
  </div>
</div>